{
  "name": "proofpack-kimi",
  "version": "1.0.0",
  "private": true,
  "description": "Evidence-backed application answers powered by Kimi K2.5.",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  },
  "engines": { "node": ">=20" }
}
